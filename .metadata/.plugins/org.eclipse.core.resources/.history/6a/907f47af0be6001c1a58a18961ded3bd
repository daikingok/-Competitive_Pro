package abc251;

import java.util.HashSet;
import java.util.Scanner;

public class AtMost {

	public static void main(String[] args) {
		// TODO 自動生成されたメソッド・スタブ
		Scanner sc = new Scanner(System.in);
		int N =sc.nextInt();
		int W = sc.nextInt();
		int[] A = new int[N];
		for(int i=0; i<N; i++) {
			A[i] = sc.nextInt();
		}
		HashSet<Intege> hash = new HashSet<Integer>()
	}

}
