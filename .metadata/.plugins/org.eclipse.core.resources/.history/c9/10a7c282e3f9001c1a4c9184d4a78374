package abc229;

import java.util.Scanner;

public class B {

	public static void main(String[] args) {
		// TODO 自動生成されたメソッド・スタブ
		Scanner sc = new Scanner(System.in);
		String a = sc.next();
		String b = sc.next();
		String[] as = a.split("");
		String[] bs = b.split("");
		Long[] a_num = new Long[a.length()];
		Long[] b_num = new Long[b.length()];
		
		for(int i=0; i<as.length; i++) {
			a_num[i] = Long.parseLong(as[i]);
//			System.out.println("a_num[i] = "+a_num[i]);
		}
		for(int i=0; i<bs.length; i++) {
			b_num[i] = Long.parseLong(bs[i]);
//			System.out.println("b_num[i] = "+b_num[i]);
		}
		//各桁が10を超える計算
		//数字が多い入力を取得
		int large_length;
		int small_len;
		if(as.length <= bs.length) {
			large_length = bs.length;
			small_len = as.length;
		}else {
			large_length = as.length;
			small_len = bs.length;
		}
//		System.out.println(large_length);
		for(int i=large_length;i>=0; i--) {
			if(hanbetu(a_num[i], b_num[small_len])) {
				System.out.println("Hard");
				break;
			}
		}

	}
	private static boolean hanbetu(long a, long b) {
		if(a + b >= 10) {
			return true;
		}else {
			return false;
		}
	}

}
